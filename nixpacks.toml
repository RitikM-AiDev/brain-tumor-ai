[phases.setup]
nixPkgs = ["python310", "python310Packages.pip", "libGL", "glib"]

[phases.install]
cmds = ["python3.10 -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"